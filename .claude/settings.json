{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && git commit -m 'chore: auto-commit after changes' 2>/dev/null || true && make build 2>/dev/null || true",
            "timeout": 120,
            "statusMessage": "Committing and building..."
          }
        ]
      }
    ]
  }
}
