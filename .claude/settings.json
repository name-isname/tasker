{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && (git diff --cached --quiet || git commit -m 'chore: auto-commit after changes') && make build",
            "timeout": 120,
            "statusMessage": "Committing and building..."
          }
        ]
      }
    ]
  }
}
